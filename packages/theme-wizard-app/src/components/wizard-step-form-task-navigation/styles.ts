import { css } from 'lit';

export default css`
  .wizard-step-form-task-navigation-icon-start > svg {
    --wizard-task-icon-size: var(--basis-size-icon-4xl);
    inline-size: var(--wizard-task-icon-size);
    block-size: var(--wizard-task-icon-size);
  }
`;
