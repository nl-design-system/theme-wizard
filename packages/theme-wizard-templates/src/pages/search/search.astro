---
import BaseLayout from '../../layouts/BaseLayout.astro';
import SearchPage from './SearchPage';

export const detail = {
  name: 'Zoeken',
  value: 'search',
  module: 'Zoeken',
};

const currentPath = Astro.url.pathname;
---

<BaseLayout title="Zoeken">
  <SearchPage client:load currentPath={currentPath} />
</BaseLayout>
