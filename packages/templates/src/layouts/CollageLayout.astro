---
import '@nl-design-system-community/ma-design-tokens/dist/theme.css';
import ClientInit from '../components/ClientInit.astro';

export interface Props {
  title?: string;
}

const isStandalone = typeof __STANDALONE_PACKAGE__ !== 'undefined' && __STANDALONE_PACKAGE__;
---

<body class={`${isStandalone ? 'ma-theme' : undefined}`}>
  <slot />
  <ClientInit />
</body>
